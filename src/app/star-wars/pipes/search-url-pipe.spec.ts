import { SearchUrlPipe } from './search-url-pipe';

describe('SearchUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
